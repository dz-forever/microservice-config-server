server:
  port: 6600
spring:
  application:
    name: microservice-consumer-movie-Feign
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:6203/eureka
  instance:
      prefer-ip-address: true
feign:
  hystrix:
    enabled: true
